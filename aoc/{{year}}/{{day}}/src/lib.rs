mod parse;

pub fn part1(input: &str) -> usize {
    let (_, numbers) = parse::numbers(input).unwrap();
    0
}

pub fn part2(input: &str) -> usize {
    let (_, numbers) = parse::numbers(input).unwrap();
    0
}
